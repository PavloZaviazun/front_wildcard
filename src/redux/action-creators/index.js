import {SET_LIBRARY, SET_VOCABULARY} from "../action-types";

export const setLibrary = (payload) => ({type: SET_LIBRARY, payload});
export const setVocabulary = (payload) => ({type: SET_VOCABULARY, payload});
